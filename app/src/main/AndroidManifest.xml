<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MedBoardApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MedBoard"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MedBoard">

        <activity android:name=".ui.main.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.topic.SystemActivity" android:exported="false" />
        <activity android:name=".ui.topic.TopicActivity" android:exported="false" />
        <activity android:name=".ui.bookmarks.BookmarksActivity" android:exported="false" />
        <activity android:name=".ui.settings.SettingsActivity" android:exported="false" />
    </application>
</manifest>
